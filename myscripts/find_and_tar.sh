#!/bin/bash

# recursively finds all HTML files in the folder and makes a zip with them.
# find . -type f -name "*.html" -exec tar -cvf htmls.tar {} \;
# find . -type f -name "*.html" | xargs -d '\n'  tar -cvzf html.zip
